# 음성 면접 시스템 디자인 시스템

## 🎨 디자인 철학

### 핵심 원칙
1. **미니멀리즘**: 불필요한 요소 제거, 핵심 기능에 집중
2. **명확성**: 사용자가 다음 단계를 직관적으로 이해
3. **신뢰감**: 전문적이고 안정감 있는 비주얼
4. **접근성**: WCAG 2.1 AA 준수, 키보드 네비게이션 지원

---

## 🎨 컬러 시스템

### Primary Colors (브랜드 컬러)
```css
/* 신뢰와 전문성을 표현하는 블루 톤 */
--primary-50: #eff6ff;
--primary-100: #dbeafe;
--primary-200: #bfdbfe;
--primary-300: #93c5fd;
--primary-400: #60a5fa;
--primary-500: #3b82f6;  /* Main Brand Color */
--primary-600: #2563eb;
--primary-700: #1d4ed8;
--primary-800: #1e40af;
--primary-900: #1e3a8a;
```

### Secondary Colors (액센트)
```css
/* 성공, 강조를 위한 그린 톤 */
--secondary-50: #f0fdf4;
--secondary-100: #dcfce7;
--secondary-200: #bbf7d0;
--secondary-300: #86efac;
--secondary-400: #4ade80;
--secondary-500: #22c55e;  /* Accent Color */
--secondary-600: #16a34a;
--secondary-700: #15803d;
--secondary-800: #166534;
--secondary-900: #14532d;
```

### Neutral Colors (배경 및 텍스트)
```css
/* 다크/라이트 모드 지원 */
--gray-50: #f9fafb;
--gray-100: #f3f4f6;
--gray-200: #e5e7eb;
--gray-300: #d1d5db;
--gray-400: #9ca3af;
--gray-500: #6b7280;
--gray-600: #4b5563;
--gray-700: #374151;
--gray-800: #1f2937;
--gray-900: #111827;
```

### Semantic Colors
```css
/* 상태 표현 */
--success: #22c55e;
--warning: #f59e0b;
--error: #ef4444;
--info: #3b82f6;
```

---

## ✍️ 타이포그래피

### 폰트 패밀리
```css
/* 한글: Pretendard (무료, 고품질) */
/* 영문: Inter (Google Fonts) */
--font-sans: 'Pretendard', 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
--font-mono: 'JetBrains Mono', 'Fira Code', monospace;
```

### 폰트 크기 (Fluid Typography)
```css
--text-xs: 0.75rem;    /* 12px */
--text-sm: 0.875rem;   /* 14px */
--text-base: 1rem;     /* 16px */
--text-lg: 1.125rem;   /* 18px */
--text-xl: 1.25rem;    /* 20px */
--text-2xl: 1.5rem;    /* 24px */
--text-3xl: 1.875rem;  /* 30px */
--text-4xl: 2.25rem;   /* 36px */
--text-5xl: 3rem;      /* 48px */
```

### 폰트 웨이트
```css
--font-normal: 400;
--font-medium: 500;
--font-semibold: 600;
--font-bold: 700;
```

---

## 📐 스페이싱 시스템

### 8px Grid System
```css
--space-1: 0.25rem;   /* 4px */
--space-2: 0.5rem;    /* 8px */
--space-3: 0.75rem;   /* 12px */
--space-4: 1rem;      /* 16px */
--space-5: 1.25rem;   /* 20px */
--space-6: 1.5rem;    /* 24px */
--space-8: 2rem;      /* 32px */
--space-10: 2.5rem;   /* 40px */
--space-12: 3rem;     /* 48px */
--space-16: 4rem;     /* 64px */
--space-20: 5rem;     /* 80px */
--space-24: 6rem;     /* 96px */
```

---

## 🔲 컴포넌트 스타일

### Buttons

#### Primary Button
```css
.btn-primary {
  background: linear-gradient(135deg, #3b82f6 0%, #2563eb 100%);
  color: white;
  padding: 0.75rem 1.5rem;
  border-radius: 0.75rem;
  font-weight: 600;
  box-shadow: 0 4px 6px -1px rgba(59, 130, 246, 0.3);
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}

.btn-primary:hover {
  transform: translateY(-2px);
  box-shadow: 0 10px 15px -3px rgba(59, 130, 246, 0.4);
}

.btn-primary:active {
  transform: translateY(0);
}
```

#### Secondary Button
```css
.btn-secondary {
  background: white;
  color: #3b82f6;
  border: 2px solid #3b82f6;
  padding: 0.75rem 1.5rem;
  border-radius: 0.75rem;
  font-weight: 600;
  transition: all 0.3s ease;
}

.btn-secondary:hover {
  background: #eff6ff;
}
```

### Cards
```css
.card {
  background: white;
  border-radius: 1rem;
  padding: 1.5rem;
  box-shadow: 0 1px 3px 0 rgba(0, 0, 0, 0.1), 0 1px 2px 0 rgba(0, 0, 0, 0.06);
  transition: all 0.3s ease;
}

.card:hover {
  box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
  transform: translateY(-4px);
}
```

### Input Fields
```css
.input {
  width: 100%;
  padding: 0.75rem 1rem;
  border: 2px solid #e5e7eb;
  border-radius: 0.5rem;
  font-size: 1rem;
  transition: all 0.2s ease;
}

.input:focus {
  outline: none;
  border-color: #3b82f6;
  box-shadow: 0 0 0 3px rgba(59, 130, 246, 0.1);
}

.input:disabled {
  background: #f9fafb;
  cursor: not-allowed;
}
```

---

## 🎬 애니메이션

### Transition Timing
```css
--transition-fast: 150ms;
--transition-base: 300ms;
--transition-slow: 500ms;

--ease-in-out: cubic-bezier(0.4, 0, 0.2, 1);
--ease-out: cubic-bezier(0, 0, 0.2, 1);
--ease-spring: cubic-bezier(0.68, -0.55, 0.265, 1.55);
```

### 주요 애니메이션

#### Fade In
```css
@keyframes fadeIn {
  from {
    opacity: 0;
    transform: translateY(10px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.fade-in {
  animation: fadeIn 0.5s ease-out;
}
```

#### Slide In From Right
```css
@keyframes slideInRight {
  from {
    opacity: 0;
    transform: translateX(100px);
  }
  to {
    opacity: 1;
    transform: translateX(0);
  }
}

.slide-in-right {
  animation: slideInRight 0.5s ease-out;
}
```

#### Pulse (녹음 중 표시)
```css
@keyframes pulse {
  0%, 100% {
    opacity: 1;
    transform: scale(1);
  }
  50% {
    opacity: 0.8;
    transform: scale(1.05);
  }
}

.pulse {
  animation: pulse 2s cubic-bezier(0.4, 0, 0.6, 1) infinite;
}
```

---

## 📱 반응형 브레이크포인트

```css
/* Mobile First Approach */
--screen-sm: 640px;   /* Small devices */
--screen-md: 768px;   /* Tablets */
--screen-lg: 1024px;  /* Laptops */
--screen-xl: 1280px;  /* Desktops */
--screen-2xl: 1536px; /* Large screens */
```

```css
/* 사용 예시 */
@media (min-width: 768px) {
  .container {
    max-width: 768px;
  }
}
```

---

## 🎭 다크 모드 (선택 사항)

### 컬러 토큰
```css
:root {
  --bg-primary: #ffffff;
  --bg-secondary: #f9fafb;
  --text-primary: #111827;
  --text-secondary: #6b7280;
}

[data-theme="dark"] {
  --bg-primary: #111827;
  --bg-secondary: #1f2937;
  --text-primary: #f9fafb;
  --text-secondary: #9ca3af;
}
```

---

## 🖼️ 레이아웃 패턴

### 메인 레이아웃
```
┌─────────────────────────────────────────┐
│           Header (Fixed)                 │
│  Logo    Navigation    User Menu         │
├─────────────────────────────────────────┤
│                                          │
│                                          │
│           Main Content                   │
│        (Max Width: 1280px)               │
│                                          │
│                                          │
├─────────────────────────────────────────┤
│              Footer                      │
└─────────────────────────────────────────┘
```

### 대시보드 레이아웃
```
┌─────────────────────────────────────────┐
│              Header                      │
├──────┬──────────────────────────────────┤
│      │                                   │
│ Side │                                   │
│ bar  │        Content Area               │
│      │                                   │
│      │                                   │
└──────┴──────────────────────────────────┘
```

---

## 🎯 페이지별 디자인 가이드

### 1. 랜딩 페이지 (/)

#### Hero Section
- **디자인**: 대형 타이포그래피 + 그라데이션 배경
- **CTA**: "무료로 시작하기" 버튼 (Primary, Large)
- **비주얼**: 3D 일러스트레이션 또는 모던한 아이콘

```
┌───────────────────────────────────────┐
│                                        │
│     AI 음성 면접으로                    │
│     취업 성공률을 높이세요               │
│                                        │
│     [무료로 시작하기]  [더 알아보기]    │
│                                        │
│           [Hero Image]                 │
│                                        │
└───────────────────────────────────────┘
```

#### Features Section
- **레이아웃**: 3-Column Grid
- **아이콘**: Lucide Icons 또는 Heroicons
- **애니메이션**: Scroll Reveal

```
┌──────────┬──────────┬──────────┐
│  [Icon]  │  [Icon]  │  [Icon]  │
│  공고분석  │  자소서   │  음성면접 │
│  AI가...  │  피드백   │  실전연습 │
└──────────┴──────────┴──────────┘
```

---

### 2. 대시보드 (/dashboard)

#### 카드 기반 레이아웃
```
┌─────────────────────────────────────────┐
│  환영합니다, 홍길동님                     │
│  오늘도 면접 준비를 시작해볼까요?          │
├─────────────┬───────────────────────────┤
│             │                           │
│ Quick Stats │   Recent Activities       │
│  공고 3개   │  - 자소서 피드백 완료      │
│  자소서 5개 │  - 면접 1회 진행           │
│  면접 2회   │                           │
│             │                           │
└─────────────┴───────────────────────────┘
```

---

### 3. 자기소개서 작성 (/cover-letter/new)

#### Step Indicator (진행 단계)
```
┌────────────────────────────────────────┐
│  ① 공고 선택  →  ② 자소서 작성  →  ③ 피드백 │
│     ●              ○              ○    │
└────────────────────────────────────────┘
```

#### 에디터
- **왼쪽**: 풍부한 텍스트 에디터 (Quill, TipTap)
- **오른쪽**: 공고 키워드 및 실시간 팁
- **하단**: "피드백 받기" CTA 버튼

```
┌───────────────┬─────────────────┐
│               │                 │
│  자소서 에디터  │  공고 키워드     │
│               │  - Python       │
│  [텍스트 입력] │  - FastAPI      │
│               │                 │
│               │  💡 TIP         │
│               │  경력을 구체적으로│
└───────────────┴─────────────────┘
      [피드백 받기]
```

---

### 4. 음성 면접 (/interview/{id})

#### 면접 진행 화면

**상단**: 진행률 바 (1/5, 2/5...)
```
Progress: ━━━━━━━━━━━━━━━━━━━━ 60% (3/5)
```

**중앙**: 질문 카드
```
┌──────────────────────────────────┐
│                                   │
│  🎙️  질문 3/5                     │
│                                   │
│  "프로젝트에서 가장 어려웠던      │
│   기술적 문제는 무엇이었나요?"    │
│                                   │
│      [🔊 질문 다시 듣기]          │
│                                   │
└──────────────────────────────────┘
```

**하단**: 녹음 컨트롤
```
┌──────────────────────────────────┐
│                                   │
│    ⏺  녹음 중...  00:45 / 01:00  │
│                                   │
│    [일시정지]  [답변 제출]         │
│                                   │
└──────────────────────────────────┘
```

#### 녹음 상태 표시
- **대기**: 회색 + "시작 준비"
- **녹음 중**: 빨간 펄스 애니메이션 + "00:45"
- **일시정지**: 주황색 + "일시정지됨"
- **완료**: 녹색 + "답변 저장됨"

---

### 5. 면접 결과 (/interview/{id}/result)

#### 종합 피드백 (상단)
```
┌─────────────────────────────────────┐
│  📊 종합 피드백                      │
│                                      │
│  전반적으로 답변이 구조적이었으나... │
│                                      │
│  강점: ✅ 논리적 사고                │
│  개선점: 💡 구체적 사례 부족         │
│                                      │
└─────────────────────────────────────┘
```

#### 개별 답변 (Accordion 형태)
```
┌─────────────────────────────────────┐
│  ▼ 질문 1: 자기소개를 부탁드립니다   │
├─────────────────────────────────────┤
│  📝 질문 텍스트                      │
│  안녕하세요, 저는...                 │
│                                      │
│  🎧 내 답변 듣기                     │
│  [▶️ 재생] 01:23                     │
│                                      │
│  💬 피드백                           │
│  자기소개가 명확하나, 핵심 역량을... │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│  ▶ 질문 2: 프로젝트 경험...          │
└─────────────────────────────────────┘
```

---

## 🎨 UI 컴포넌트 라이브러리

### 추천 라이브러리

#### 1. Shadcn/ui (최우선 추천)
- **장점**: Tailwind 기반, 커스터마이징 용이, 복사-붙여넣기 방식
- **URL**: https://ui.shadcn.com/
- **사용 컴포넌트**: Button, Card, Dialog, Form, Select, Textarea

#### 2. Headless UI
- **장점**: 완전한 접근성, 스타일 자유도
- **URL**: https://headlessui.com/
- **사용 컴포넌트**: Dropdown, Modal, Tabs

#### 3. Framer Motion
- **장점**: 부드러운 애니메이션
- **URL**: https://www.framer.com/motion/
- **사용**: 페이지 전환, 카드 호버 효과

---

## 🎤 음성 관련 UI 패턴

### 오디오 플레이어
```jsx
// 커스텀 오디오 플레이어 디자인
┌──────────────────────────────────┐
│  ▶️  ━━━━━●━━━━━━  01:23 / 02:15 │
│                                   │
│  [1.0x]  [다운로드]               │
└──────────────────────────────────┘
```

### 녹음 시각화 (Waveform)
```
┌──────────────────────────────────┐
│                                   │
│  ⏺  | | | || ||| || | |  00:32   │
│                                   │
└──────────────────────────────────┘
```

라이브러리: `wavesurfer.js` 또는 `canvas`로 직접 구현

---

## ✨ 마이크로 인터랙션

### 버튼 클릭
```css
/* Ripple Effect */
.btn:active::after {
  content: '';
  position: absolute;
  background: rgba(255, 255, 255, 0.5);
  border-radius: 50%;
  animation: ripple 0.6s ease-out;
}

@keyframes ripple {
  from {
    width: 0;
    height: 0;
    opacity: 1;
  }
  to {
    width: 100px;
    height: 100px;
    opacity: 0;
  }
}
```

### 로딩 상태
```jsx
// Skeleton Loading
<div className="animate-pulse">
  <div className="h-4 bg-gray-200 rounded w-3/4"></div>
  <div className="h-4 bg-gray-200 rounded w-1/2 mt-2"></div>
</div>
```

### 토스트 알림
```
┌────────────────────────────┐
│  ✅ 자소서가 저장되었습니다 │
└────────────────────────────┘
```

라이브러리: `react-hot-toast` 또는 `sonner`

---

## 🌟 고급 디자인 기법

### Glassmorphism (유리 효과)
```css
.glass {
  background: rgba(255, 255, 255, 0.1);
  backdrop-filter: blur(10px);
  border: 1px solid rgba(255, 255, 255, 0.2);
  border-radius: 1rem;
}
```

### Neumorphism (부드러운 입체감)
```css
.neumorphic {
  background: #f0f0f0;
  box-shadow: 
    8px 8px 16px #d1d1d1,
    -8px -8px 16px #ffffff;
  border-radius: 1rem;
}
```

### 그라데이션 텍스트
```css
.gradient-text {
  background: linear-gradient(135deg, #3b82f6 0%, #8b5cf6 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}
```

---

## 📊 데이터 시각화

### 진행률 표시
```jsx
// Circular Progress
<svg viewBox="0 0 100 100">
  <circle
    cx="50"
    cy="50"
    r="40"
    stroke="#e5e7eb"
    strokeWidth="8"
    fill="none"
  />
  <circle
    cx="50"
    cy="50"
    r="40"
    stroke="#3b82f6"
    strokeWidth="8"
    fill="none"
    strokeDasharray={`${progress * 2.51} 251`}
    transform="rotate(-90 50 50)"
  />
</svg>
```

### 통계 카드
```
┌──────────────┐
│  📈          │
│  면접 완료    │
│     12회     │
│  +3 이번주   │
└──────────────┘
```

---

## 🎯 접근성 (Accessibility)

### ARIA 레이블
```jsx
<button aria-label="음성 녹음 시작">
  <MicIcon />
</button>

<div role="status" aria-live="polite">
  녹음이 시작되었습니다
</div>
```

### 키보드 네비게이션
- `Tab`: 다음 요소로 이동
- `Shift + Tab`: 이전 요소로 이동
- `Enter/Space`: 버튼 활성화
- `Esc`: 모달 닫기

### 포커스 스타일
```css
*:focus {
  outline: 2px solid #3b82f6;
  outline-offset: 2px;
}
```

---

## 📱 모바일 최적화

### 터치 타겟 크기
```css
/* 최소 44x44px (Apple HIG) */
.touch-target {
  min-width: 44px;
  min-height: 44px;
  padding: 12px;
}
```

### Safe Area (아이폰 노치 대응)
```css
.container {
  padding-left: env(safe-area-inset-left);
  padding-right: env(safe-area-inset-right);
  padding-bottom: env(safe-area-inset-bottom);
}
```

---

## 🚀 성능 최적화

### 이미지 최적화
- **형식**: WebP + AVIF (fallback: PNG/JPG)
- **로딩**: Lazy Loading
- **크기**: Next.js Image 또는 Cloudinary 사용

### 폰트 최적화
```css
/* 폰트 로딩 전략 */
@font-face {
  font-family: 'Pretendard';
  font-display: swap; /* FOUT 방지 */
  src: url('/fonts/Pretendard-Regular.woff2') format('woff2');
}
```

### 코드 스플리팅
```jsx
// React Lazy Loading
const InterviewPage = lazy(() => import('./pages/Interview'));
```

---

## 🎨 디자인 도구 및 리소스

### 디자인 도구
- **Figma**: UI/UX 디자인 및 프로토타입
- **Excalidraw**: 플로우차트 및 와이어프레임

### 아이콘
- **Lucide Icons**: https://lucide.dev/
- **Heroicons**: https://heroicons.com/
- **Phosphor Icons**: https://phosphoricons.com/

### 일러스트레이션
- **unDraw**: https://undraw.co/
- **Storyset**: https://storyset.com/
- **3D Icons**: https://3dicons.co/

### 애니메이션
- **LottieFiles**: https://lottiefiles.com/
- **Rive**: https://rive.app/

---

## 📋 체크리스트

### 디자인 품질 체크
- [ ] 모든 텍스트가 읽기 쉬운가? (최소 16px)
- [ ] 색상 대비가 충분한가? (WCAG AA: 4.5:1)
- [ ] 모바일에서 잘 보이는가?
- [ ] 로딩 상태가 명확한가?
- [ ] 에러 메시지가 친절한가?
- [ ] 성공/실패 피드백이 즉각적인가?
- [ ] 키보드만으로 모든 기능을 사용할 수 있는가?

---

**작성일**: 2025-11-14  
**버전**: 1.0.0  
**디자이너**: AI Assistant

